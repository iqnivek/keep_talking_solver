import React, {
  StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
  backgroundRed: {
    backgroundColor: 'red',
  },
  header: {
    paddingTop: 30,
  },
  textWhite: {
    color: '#fff',
  },
});

export default styles;
